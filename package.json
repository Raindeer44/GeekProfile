{
  "name": "geekprofile",
  "version": "0.5.3",
  "description": "A geek profile maker using markdown.",
  "main": "",
  "scripts": {
    "bootstrap": "pagic profile",
    "build": "cd profile && pagic build",
    "release": "npm run build; mkdir -p ./docs; cp -rf profile/public/* ./docs",
    "clean": "rm -rf ./docs; rm -rf ./profile/public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jade Thornton",
  "repository": "https://github.com/raindeer44/geekprofile",
  "license": "MIT",
  "devDependencies": {
    "pagic": "^0.5.0"
  }
}
